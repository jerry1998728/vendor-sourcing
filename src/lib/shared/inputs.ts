/**
 * Limits shared by the Vendor Source form and the routes it posts to, so the
 * hint on a disabled button and the server's validation never disagree.
 */
export const REQUIREMENT_MIN_CHARS = 10;
export const REQUIREMENT_MAX_CHARS = 2000;
